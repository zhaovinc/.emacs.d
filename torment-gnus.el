(require 'socks)

;; use a socks-over-ssh tunnel in sap office
;; (setq socks-noproxy '("localhost"))
;; (setq socks-override-functions 1)
;; (setq socks-server '("sapssh" "pvgd50802874a" "55555" 5))

(setq gnus-select-method '(nntp "news.gmane.org"))

(setq user-full-name "zegal") 
(setq user-mail-address "ravensore@gmail.com")
